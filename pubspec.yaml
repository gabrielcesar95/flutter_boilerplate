name: flutter_boilerplate
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  mdi: ^1.0.1
  dio: ^3.0.8
  mobx: ^1.0.1
  flutter_mobx: ^1.0.1
  flutter_modular: ^2.0.0
  oauth2: ^1.6.0
  hive: ^1.4.0
  hive_flutter: ^0.3.0
  flutter_dotenv: ^2.1.0

dev_dependencies:
  mockito: ^4.1.1
  mobx_codegen: ^1.0.1
  build_runner: ^1.7.4
  build_test: ^1.2.1
  build_web_compilers: ^2.11.0
  hive_generator: any

flutter:
  uses-material-design: true
  assets:
    - assets/img/logo.png
    - .env

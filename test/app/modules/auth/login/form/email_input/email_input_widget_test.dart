main() {
//  testWidgets('EmailInputWidget has message', (WidgetTester tester) async {
//    await tester.pumpWidget(buildTestableWidget(EmailInputWidget()));
//    final textFinder = find.text('EmailInput');
//    expect(textFinder, findsOneWidget);
//  });
}

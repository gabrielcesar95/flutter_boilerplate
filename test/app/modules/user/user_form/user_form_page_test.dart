import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:flutter_boilerplate/app/modules/user/user_form/user_form_page.dart';

void main() {
  testWidgets('UserFormPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(UserFormPage(title: 'UserForm')));
    //  final titleFinder = find.text('UserForm');
    //  expect(titleFinder, findsOneWidget);
  });
}

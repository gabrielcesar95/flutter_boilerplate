import 'package:flutter_modular_test/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:flutter_boilerplate/app/modules/user/user_module.dart';

void main() {
  initModule(UserModule());
  // UserFormController userform;
  //
  setUp(() {
    //     userform = UserModule.to.get<UserFormController>();
  });

  group('UserFormController Test', () {
    //   test("First Test", () {
    //     expect(userform, isInstanceOf<UserFormController>());
    //   });

    //   test("Set Value", () {
    //     expect(userform.value, equals(0));
    //     userform.increment();
    //     expect(userform.value, equals(1));
    //   });
  });
}
